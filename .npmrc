; Install the local `file:` dependency (@igor/eslint-config-typescript) as a
; copied package rather than a symlink, so its peer deps resolve from this
; project's node_modules instead of the source directory's (empty) tree.
install-links=true
